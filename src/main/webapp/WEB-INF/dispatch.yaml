dispatch:
  # Default service serves the typical web resources and all static resources.
  - url: "*/favicon.ico"
    service: default

  # Default service serves simple hostname request.
  - url: "lespickup.appspot.com/"
    service: default

  # Send all work to the one static backend.
  - url: "*/api/*"
    service: default